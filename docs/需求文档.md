# kNews Desktop — 软件需求规格说明书 (SRS)

> 版本: 1.2 | 日期: 2026-05-10 | 状态: Draft

---

## 1. 引言

### 1.1 编写目的

本文档定义 kNews Desktop 桌面应用的完整需求，作为开发、测试和验收的依据。面向开发团队、项目管理者及相关利益方。

### 1.2 项目概述

kNews Desktop 是一款**跨平台桌面新闻聚合应用**，聚合 60+ 新闻源，提供本地定时刷新、MCP 协议接入（供 AI 工具调用新闻数据）、双模式登录（GitHub OAuth / Email Magic Link）等能力。支持 Windows 和 Mac 双平台安装包。应用采用缓存优先策略加载数据，减少不必要的网络请求和 UI 闪烁，并提供基于用户等级的功能权限控制。

### 1.3 术语与缩写

| 术语 | 定义 |
|------|------|
| MCP | Model Context Protocol，AI 工具通信协议 |
| Magic Link | 包含登录令牌的邮件链接，用户点击即可完成认证 |
| Deep Link | 自定义 URL 协议（`knews://`），用于从外部唤起应用 |
| IPC | Inter-Process Communication，Electron 进程间通信 |
| Repository | 数据访问层封装模式，隔离 SQL 细节 |
| 策略模式 | 运行时可替换的算法封装模式 |
| 模板方法模式 | 基类定义算法骨架、子类实现具体步骤的设计模式 |
| 缓存优先 | 优先使用本地缓存数据，过期后台静默刷新的加载策略 |

---

## 2. 总体描述

### 2.1 产品视角

kNews Desktop 是一个**独立的桌面应用产品**，同时对外提供 MCP HTTP 服务接口，供 Claude Desktop、Cursor 等 AI 工具消费新闻数据。

```
┌────────────────────────────────────────────────────────┐
│                   kNews Desktop                         │
│                                                         │
│  ┌─────────────┐  IPC   ┌──────────────────────────┐   │
│  │  Vue 3 前端  │◄──────►│  Electron Main Process   │   │
│  │  (Renderer)  │       │                          │   │
│  │             │       │  ┌─ 认证系统 (OAuth/      │   │
│  │  新闻浏览    │       │  │       Magic Link)     │   │
│  │  源管理      │       │  ├─ 爬虫引擎 (60+源)     │   │
│  │  源管理      │       │  ├─ MCP Server           │───┼──► AI 工具
│  │  用户设置    │       │  └─ 用户管理             │   │
│  └─────────────┘       └──────────┬───────────────┘   │
│                                   │                    │
│                        ┌──────────▼──────────┐        │
│                        │  Supabase PostgreSQL │        │
│                        │  (云端数据库)         │        │
│                        └─────────────────────┘        │
└────────────────────────────────────────────────────────┘
```

### 2.2 用户角色

| 角色 | 描述 | 核心场景 |
|------|------|----------|
| 普通用户 | 桌面端新闻浏览者 | 浏览新闻、管理偏好 |
| API 用户 | 通过 MCP 接入的 AI 工具 | 获取新闻数据、查询可用源 |
| 管理员 | 系统管理者 | 管理源、用户、API Key |

### 2.3 运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10+ 或 macOS 11+ |
| 网络要求 | 首次使用及数据库操作需网络连接 |
| 硬盘空间 | ≥ 200MB |
| 外部服务 | Supabase 云数据库、GitHub OAuth |

---

## 3. 功能需求

### 3.1 F01 — 桌面应用框架

**描述**: Electron 桌面应用的整体骨架，包括窗口管理、进程通信和打包发布。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F01-01 | 创建 Electron 主窗口，最小宽度 1024px，最小高度 700px | P0 |
| F01-02 | 按领域分组的 IPC 通信层（auth/feeds/sources/scraper/user/mcp/chat），通过 preload contextBridge 安全暴露 | P0 |
| F01-03 | 支持 Windows (NSIS .exe) 和 Mac (.dmg) 安装包打包 | P0 |
| F01-04 | 启动时按序初始化：Supabase 连接 → 数据库建表 → 爬虫启动 → MCP 启动 → Session 恢复；已有本地缓存时 bootstrap 在后台异步执行，不阻塞界面加载 | P0 |
| F01-05 | 关闭窗口时清理所有子系统（定时器、MCP Server） | P0 |
| F01-06 | 主进程启动时必须根据运行模式自动加载环境变量：开发模式优先从当前工作目录读取 `.env`/`.env.<NODE_ENV>`，打包模式需按 `process.resourcesPath`、可执行文件所在目录和当前工作目录依次查找 `.env.production`，未命中时再回退 `.env` | P0 |
| F01-07 | 打包配置必须将生产环境变量文件（`.env.production`）作为应用资源一并分发，避免安装包运行后因工作目录变化导致 Supabase、GLM 等运行时配置缺失 | P0 |
| F01-08 | 应用需提供统一品牌图标资产，并在安装包、Electron 主窗口、macOS Dock 与 Renderer favicon 等入口保持一致的 logo 展示 | P1 |

### 3.2 F02 — 认证系统

**描述**: 支持双模式登录（GitHub OAuth / Email Magic Link），采用策略模式 + 工厂模式 + 模板方法模式实现。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F02-01 | AuthTemplate 基类定义认证骨架：initiate → validate → syncUser → createSession | P0 |
| F02-02 | GitHub OAuth 策略：临时 HTTP Server 接收 callback，shell.openExternal 打开浏览器，5 分钟超时 | P0 |
| F02-03 | Email Magic Link 策略：调用 Supabase `signInWithOtp` 发送含登录链接的邮件，用户点击链接完成认证 | P0 |
| F02-04 | 注册 Electron 自定义协议 `knews://`，捕获 `knews://auth/callback?token=xxx` Deep Link 完成登录 | P0 |
| F02-05 | AuthFactory 按 type 字符串创建对应策略实例（`github` / `magic-link`），未知类型抛错 | P0 |
| F02-06 | Session 通过 Electron safeStorage 加密持久化，重启自动恢复 | P0 |
| F02-07 | 登出时清除 Session 并通知前端重置 | P1 |

### 3.2.1 F02-A — 登录模态框

**描述**: 独立的登录模态框组件，整合 Magic Link 和 GitHub 两种登录方式，替代旧的 popover 内嵌 OTP 表单。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F02-A-01 | 未登录时 header 右侧显示"登录"文字按钮（替代 GitHub 图标圆形按钮），已登录显示用户头像 | P0 |
| F02-A-02 | 模态框布局参照设计稿，包含：标题"安全访问"、邮箱输入 + "发送验证码"渐变按钮、分割线、GitHub 登录按钮、底部隐私链接 | P0 |
| F02-A-03 | 模态框支持点击遮罩区域或 Escape 键关闭，带淡入淡出过渡动画 | P0 |
| F02-A-04 | Magic Link 流程：输入邮箱 → 调用 IPC `sendMagicLink` → 显示"请查收邮件"状态 → 支持 Deep Link 回调自动关闭模态框 | P0 |
| F02-A-05 | GitHub 登录按钮关闭模态框后触发 GitHub Device Flow（复用现有 `github-device-dialog`） | P0 |
| F02-A-06 | 登录后头像按登录类型显示：GitHub 用户显示 GitHub 头像，邮箱用户显示 `i-tabler-user` 图标 | P1 |

### 3.3 F03 — 用户管理

**描述**: 用户生命周期管理，首次登录自动创建，信息同步更新。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F03-01 | 用户表：id(BIGINT IDENTITY)/email/github_id/nickname/level/balance/created_at/updated_at | P0 |
| F03-02 | GitHub 首次登录按 github_id 查找或创建用户 | P0 |
| F03-03 | Email (Magic Link) 首次登录按 email 查找或创建用户（nickname 取邮箱前缀） | P0 |
| F03-04 | 重复登录更新 updated_at，GitHub 信息变更时同步 nickname | P0 |

### 3.4 F04 — 数据库层

**描述**: 基于 Supabase PostgreSQL 的数据存储，采用 Repository 模式封装数据访问。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F04-01 | Supabase 客户端全局单例，环境变量配置 | P0 |
| F04-02 | 幂等 SQL 迁移脚本，按序创建 users/api_key/source/cache/preference 五张表 | P0 |
| F04-03 | 每个 Repository class 封装表的 CRUD，构造函数注入 Supabase 客户端 | P0 |
| F04-04 | API Key 表：id/user_id/key_hash/name/is_active(is_default)/source_scope/rate_limit/max_count/last_used/call_count | P0 |
| F04-05 | Source 表：id/name/url/category/enabled/config | P0 |
| F04-06 | Cache 表：id/source_id/data(JSONB)/updated_at | P0 |
| F04-07 | Preference 表：id/user_id/data(JSONB)/updated_at | P1 |

### 3.5 F05 — 新闻爬虫引擎

**描述**: 定时抓取 60+ 新闻源，并发控制，缓存管理。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F05-01 | ScraperEngine 定时器，默认 10 分钟刷新全部已启用源 | P0 |
| F05-02 | 支持手动全源刷新和单源刷新 | P0 |
| F05-03 | p-limit 并发控制，最大并发 5，单源超时 30 秒 | P0 |
| F05-04 | SourceFetcher 支持 RSS 解析和 Cheerio HTML 抓取，返回统一格式 (title/url/date/source) | P0 |
| F05-05 | 成功写入 Supabase cache 表，失败保留上次缓存并记录日志 | P0 |
| F05-06 | 全局关闭（`enabled = false`）的数据源不得进入任何定时刷新、手动刷新、热点问答补拉或上下文注入链路；系统需在调度前与执行前都进行启用态校验，并记录跳过原因 | P0 |

### 3.6 F06 — 本地 MCP Server

**描述**: 内嵌 HTTP Server，对外提供 MCP 协议服务。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F06-01 | HTTP Server 默认端口 12580，占用时递增，实际端口写入配置 | P0 |
| F06-02 | get_available_sources 工具：返回 API Key 允许访问的源列表 | P0 |
| F06-03 | get_hotest_latest_news 工具：返回可稳定映射为结构化 JSON 的指定源最新新闻，至少覆盖稳定标识、标题、链接、来源和时间字段 | P0 |
| F06-04 | API Key 认证：Authorization Header 验证，更新 last_used/call_count | P0 |
| F06-05 | 频率限制：按 rate_limit 限制每小时请求数，超限返回 429 | P0 |
| F06-06 | 健康检查：GET /mcp/health 返回 status/uptime/port | P1 |

### 3.7 F07 — 缓存优先数据加载

**描述**: 数据加载优先使用文件系统缓存，减少不必要的网络请求和 UI 闪烁，认证状态变化不触发数据重载。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F07-01 | 数据加载时检查每个数据源的缓存状态（是否存在、fetchedAt 时间戳），基于缓存新鲜度决定获取行为 | P0 |
| F07-02 | 所有选中数据源均有缓存且未过期（≤1 小时）时，直接使用缓存数据构建 boards，不显示 loading 骨架屏，不发起新的网络请求 | P0 |
| F07-03 | 部分或全部缓存过期（>1 小时）时，先展示缓存数据，后台静默刷新过期源，刷新完成后无闪烁更新 boards | P0 |
| F07-04 | 无缓存时走正常获取流程，显示 loading 状态，获取完成后写入缓存 | P0 |
| F07-05 | 用户登录/登出（authToken 变化）不触发 `buildBoards()` 全量重建，仅重新加载用户偏好 | P0 |
| F07-06 | 偏好变化影响 boards（如关注列表变化）时，由偏好 watcher 自身触发数据更新 | P1 |
| F07-07 | 首页 store 按 tab 维度独立存储 boards（boardsByTab/sourcesByTab/loadingByTab），tab 切换不覆盖其他 tab 数据 | P0 |
| F07-08 | feed-service 批量查询返回 `{ items, fetchedAt }` 结构，支持缓存新鲜度判断 | P0 |
| F07-09 | 本地缓存偏好按用户 ID 隔离（`knews:board-preferences:${userId}`），切换账号不串数据 | P0 |

### 3.7.1 F07-A — 非阻塞启动

**描述**: 已有本地缓存的用户启动应用时跳过初始化屏，直接展示缓存数据；首次用户仍显示初始化屏。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F07-A-01 | 启动时通过 `hasLocalCache()` 检查 `~/.knews/cache/*.json`，有缓存时直接加载 renderer，无缓存时显示初始化屏 | P0 |
| F07-A-02 | 主进程在 `createWindow()` 后立即注册 `bootstrap:status` IPC handler 和 fallback handler（覆盖 `auth:getSession`/`sources:list` 等），bootstrap 未完成时返回 `{ error: 'Service not ready', retry: true }` | P0 |
| F07-A-03 | bootstrap 完成后移除 fallback handler 并注册正式 IPC handler，发送 `bootstrap:done` 事件通知 renderer | P0 |
| F07-A-04 | bootstrap 失败时发送 `bootstrap:error` 事件；首次用户显示错误屏，回访用户仅 toast 提示并保持缓存数据可见 | P0 |
| F07-A-05 | ScraperEngine 调度改为 `schedule({ forceInitialScrape: false })` 缓存优先模式，不强制首次抓取 | P0 |
| F07-A-06 | renderer 挂载时通过 `bootstrapApi.status()` 检查就绪状态，未就绪时显示加载态，监听 `bootstrap:done` 后加载缓存数据 | P0 |

### 3.7.2 F07-B — 一键刷新进度反馈

**描述**: 一键刷新期间按钮显示实时进度和旋转动画，完成后弹出 toast 反馈。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F07-B-01 | 刷新按钮在刷新中显示旋转图标 + "已刷新 N/M"（M 为同时排除用户偏好 disabledSourceIds 和管理员全局禁用的数据源总数），按钮进入 disabled 状态 | P0 |
| F07-B-02 | 逐源 `Promise.allSettled` 并发刷新，每完成一个源通过 `knews:refresh-progress` CustomEvent 更新进度 | P0 |
| F07-B-03 | 全部完成后恢复按钮，0 失败时 toast "刷新完成"，有失败时 toast "N 个数据源刷新失败" | P0 |

### 3.7.3 F07-C — 用户数据源偏好

**描述**: 用户切换数据源可见性写入 preference 表的 disabledSourceIds，不再修改全局 source 表；管理员全局禁用的数据源在用户设置页灰显且不可操作。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F07-C-01 | 用户在设置页切换数据源可见性时，写入 preference 表 `board.disabledSourceIds` 数组，不修改全局 source 表的 enabled 字段 | P0 |
| F07-C-02 | 管理员全局禁用（`source.enabled = false`）的数据源在用户设置页灰显（降低透明度）且不可操作 | P0 |
| F07-C-03 | 首页 buildBoards 数据源过滤同时应用全局 enabled 和用户偏好 disabledSourceIds | P0 |
| F07-C-04 | 远端偏好写入条件改为检查 `profile.userId`，兼容 Magic Link 登录（authToken 为空）的场景 | P0 |
| F07-C-05 | IPC 传输 disabledSourceIds 时展开为普通数组，避免 Vue Proxy 无法序列化的问题 | P1 |

### 3.8 F08 — 手动刷新权限控制

**描述**: 手动"一键刷新"按钮基于用户等级进行权限控制。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F08-01 | 手动刷新按钮仅对已登录且 `user.level >= 1` 的用户可见和可用 | P0 |
| F08-02 | 已登录但 level < 1 的用户点击刷新时，显示升级订阅提示（toast 通知），同时导航到订阅页面（F09） | P1 |
| F08-03 | 未登录用户显示刷新按钮，点击时 toast 提示"请先登录" | P1 |

### 3.9 F09 — 订阅页面

**描述**: 独立的三级定价订阅页面，参照 `docs/subscribe.html` 设计稿，支持用户查看和选择方案。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F09-01 | 订阅页面展示三级定价卡（Free 0 元 / Plus 5 元月 / Pro 15 元月），每卡包含方案名称、价格、功能列表、操作按钮 | P0 |
| F09-02 | 当前方案及所有低级方案卡片高亮并禁用操作按钮；当前方案显示"当前方案"，低级方案显示"已包含" | P0 |
| F09-03 | 未登录用户点击升级按钮提示先登录 | P0 |
| F09-04 | Free 方案功能列表：3 次 MCP 请求/小时、基础新闻浏览、受限 API 访问 | P0 |
| F09-05 | Plus 方案功能列表：100 次 MCP 请求/小时、一键手动刷新、邮件支持；Plus 卡片显示"最受欢迎"标签 | P0 |
| F09-06 | Pro 方案功能列表：不限次数 MCP 请求、优先 API 路由、专属客户管理、自定义安全协议 | P0 |
| F09-07 | 用户菜单 popover 新增"订阅方案"入口，附带当前方案标签（Free/Plus/Pro） | P0 |
| F09-08 | 升级确认对话框：显示目标方案名称和价格，确认按钮点击后进入 loading 状态（3s），完成后显示成功反馈 | P0 |
| F09-09 | 升级成功后更新 userStore.profile.level，刷新订阅页面当前方案高亮 | P0 |

### 3.10 F10 — 订阅管理（后端）

**描述**: 后端订阅管理，包括 subscriptions 表、SubscriptionRepository、SubscriptionService 及 IPC 通道。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F10-01 | Subscriptions 表：id(TEXT PK)/user_id(BIGINT FK)/plan('free'/'plus'/'pro')/level(0/1/2)/status('active'/'expired')/started_at/expires_at/created_at/updated_at | P0 |
| F10-02 | SubscriptionRepository：findActiveByUserId、create、expireById 方法 | P0 |
| F10-03 | SubscriptionService.createSubscription：旧订阅标记 expired → 模拟支付(3s) → 创建新订阅 → 更新 users.level → 同步 MCP API Key 权限 | P0 |
| F10-04 | SubscriptionService.getCurrentSubscription：返回当前有效订阅，无则返回 Free 默认信息 | P0 |
| F10-05 | Level 与 MCP 权限映射：level 0 → rate_limit=3/max_count=10；level 1 → rate_limit=100/max_count=50；level 2 → rate_limit=-1/max_count=-1 | P0 |
| F10-06 | 防止重复订阅：用户已有 active 相同 plan 订阅时返回当前订阅，不创建新记录 | P1 |
| F10-07 | IPC 通道 subscription:getCurrent：返回当前用户订阅信息 | P0 |
| F10-08 | IPC 通道 subscription:create：执行模拟支付(3s)后创建订阅并返回结果 | P0 |

### 3.11 F11 — 模拟支付

**描述**: 使用 setTimeout 模拟支付流程，封装为独立 PaymentService 以便后续替换为真实支付网关。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F11-01 | PaymentService.processPayment：setTimeout 3000ms 模拟延迟，返回 `{ success: true, transactionId }` | P0 |
| F11-02 | 支付进行中前端确认按钮显示 loading 状态（"处理中..."），禁止重复点击 | P0 |
| F11-03 | 支付成功后前端更新 userStore.profile.level，关闭对话框，显示成功 toast | P0 |
| F11-04 | PaymentService 接口抽象为独立模块，后续可替换为真实支付网关无需改动调用方 | P1 |

### 3.12 F12 — AI 对话助手界面

**描述**: 新增独立的 `K-Ai 对话助手` 页面，支持精简侧边栏、多会话切换、消息交互和热点快捷入口，视觉参考 Stitch 屏幕 `K-Ai 对话助手 (精简侧边栏)`。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F12-01 | 系统提供独立 `/assistant` 路由页面，包含精简侧边栏、会话列表、消息主区域、输入区和热点快捷区 | P0 |
| F12-02 | 系统提供进入 AI 对话助手页面的全局入口，用户可从应用导航进入聊天页面 | P0 |
| F12-03 | 用户点击”新建对话”时创建空会话并聚焦输入框 | P0 |
| F12-04 | 用户发送消息后立即显示用户消息、禁用重复提交并展示 assistant 等待状态 | P0 |
| F12-05 | assistant 回复成功后追加到当前会话并滚动到最新消息位置 | P0 |
| F12-06 | 侧边栏按最近更新时间倒序展示历史会话，支持切换查看完整历史 | P0 |
| F12-07 | 页面展示一组基于当前热点数据的快捷问题或摘要，支持用户一键围绕热点发起对话 | P1 |
| F12-08 | 聊天页面在窄屏下仍保留会话切换与消息发送能力，可折叠侧边栏或切换为抽屉布局 | P1 |
| F12-09 | 流式打字机效果：LLM 使用 `stream: true` 调用，逐 token 通过 IPC 推送到前端，assistant 消息气泡中逐字显示回复内容 | P0 |
| F12-10 | Agent 状态展示：获取热点数据时逐源上报进度状态（如”正在调用 MCP 获取【抖音】热点...”），前端实时显示当前操作；热点获取完成后状态指示器消失，切换为打字机输出 | P0 |
| F12-11 | 可点击新闻卡片：AI 回复中的新闻条目（markdown 链接格式）渲染为可交互卡片组件（左侧蓝色竖线指示），点击在系统浏览器中打开原文 | P0 |
| F12-12 | 流式中断：AI 生成过程中发送按钮替换为停止按钮（方块图标），用户点击后终止流式响应，已接收内容保留并标记”已中断” | P0 |
| F12-13 | 会话 5 分钟阈值：页面加载时检查最近会话的 `updatedAt`，5 分钟内自动恢复该会话，否则展示空白新对话页 | P1 |
| F12-14 | 热点消息气泡继续沿用当前已上线的紧凑新闻卡片布局，视觉参考 `docs/k-ai-screen.html` 中的气泡新闻区域；本次能力迭代不得改动既有卡片样式和点击交互 | P1 |
| F12-15 | 热点拉取状态以“追加事件流”形式展示多个渠道的实时进度，而不是只在单条文案中替换当前渠道名称；用户可连续看到每个渠道的开始、完成、跳过或失败记录 | P0 |
| F12-16 | 系统在 assistant 回复期间展示实时 Thinking 区，仅显示当前流式产生的思考片段；最终完成后不自动展开完整 Thinking 历史 | P1 |
| F12-17 | 热点新闻卡片必须直接基于聊天侧提供的 MCP JSON 结构化数据渲染标题、来源、时间和链接，不得依赖正文 markdown 或工具文本反向解析卡片信息 | P0 |
| F12-18 | 每条热点新闻在标题下支持一句话描述区域；描述未返回时展示加载动画，收到对应补丁后原位更新，不影响其他新闻项或主回答内容 | P1 |
| F12-19 | K-Ai 助手在当前单客户端单流架构下，同一时间仅允许一个会话处于流式响应中；若用户在流式响应期间再次发送消息、新建会话、切换会话或删除会话，前端必须阻止操作并显示统一错误提示 | P0 |
| F12-20 | 用户删除历史会话前必须经过二次确认，确认框需包含会话标题或等价识别信息，并明确提示“此操作不可撤销” | P1 |
| F12-21 | 热点速览卡中的新闻序号块应使用对应数据源的主题色渲染，并自动保证数字文本在浅/深色来源色上的可读性 | P1 |
| F12-22 | 当热点速览卡后接正文，或消息记录较多需要滚动时，助手界面应保持紧凑阅读节奏：卡片与正文有清晰但不过大的间距，滚动到底部时最后一条消息不得距离输入区过远 | P1 |

### 3.13 F13 — GLM 对话编排

**描述**: 由 Main Process 统一编排 GLM 模型请求，并在热点问答场景下复用现有本地 MCP 服务能力增强上下文。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F13-01 | 聊天服务通过兼容 Chat Completions 协议的接口发起请求，默认模型为 `glm-4.7` | P0 |
| F13-02 | GLM API Base URL、API Key 和模型名通过运行时配置提供，前端不可直接读取密钥 | P0 |
| F13-03 | 当用户点击热点快捷入口或命中热点/新闻问答场景时，系统必须使用用户 default MCP API Key 按 MCP 标准流程请求本地 `/mcp` 端点获取最新热点上下文后再请求模型 | P0 |
| F13-04 | 注入给模型的热点上下文以及推送给前端的热点卡片数据都必须来源于 MCP 结构化结果，且至少包含稳定标识、新闻标题、链接、来源标识和可用时间信息 | P0 |
| F13-05 | 系统提供 `chat` 领域 IPC 能力，覆盖：`chat:listSessions`、`chat:getSession`、`chat:createSession`、`chat:sendMessage`、`chat:sendMessageStream`（流式发送）、`chat:streamEvent`（流式事件推送，事件类型：status/token/done/error）、`chat:abortStream`（中断流式响应）、`chat:deleteSession` | P0 |
| F13-06 | GLM 请求超时、鉴权失败或服务异常时，前端收到用户可理解的错误信息，且既有会话消息不丢失 | P0 |
| F13-07 | MCP 热点获取失败时，系统允许对话降级为普通聊天，并提示热点上下文暂不可用 | P1 |
| F13-08 | 流式响应：LLM 调用使用 `stream: true`，Main Process 解析 SSE 逐 token 推送到前端；流式中断支持 AbortController 链路（前端 stop → IPC → 终止 HTTP 请求） | P0 |
| F13-09 | AI 对话获取热点数据时不得直接访问 `LocalCacheRepository`、`FeedService` 或 `ScraperEngine` 组装热点，而应完全使用本地 MCP 工具返回结果作为热点来源 | P0 |
| F13-10 | AI 对话热点请求必须受到 default MCP API Key 的 `source_scope`、`max_count` 与 `rate_limit` 约束；当 key 无效、超限或工具调用失败时，系统允许降级为普通对话并提示热点上下文暂不可用 | P0 |
| F13-11 | 注册时自动创建内部 MCP API Key：用户首次注册/登录后，系统自动创建一条 `is_default = true` 的 MCP API Key，名称为 UUID，默认数据源为抖音/微博/GitHub trending/今日头条，限流参数取当前 level 对应最大值；该 Key 不可被用户删除（前端隐藏删除按钮），但允许修改数据源范围；用户等级升降时限流参数随订阅同步更新 | P0 |
| F13-12 | 当命中热点请求时，模型输出除新闻事实外，还应包含见解、脉络梳理、趋势判断、潜在影响和不确定性说明；除非用户明确要求，仅返回新闻列表不视为满足需求 | P0 |
| F13-13 | 聊天流式协议支持与正文分离的 Thinking 增量事件，前端仅消费实时新增片段，不回放本轮完整 Thinking 文本 | P1 |
| F13-14 | 热点数据候选源需先经过全局 enabled 状态过滤，再与用户 API Key scope、缓存命中与刷新策略叠加求交集；任一环节发现源已禁用时，系统不得发起网络请求 | P0 |
| F13-15 | AI 对话在单次热点请求中必须先通过 MCP 获取允许访问的数据源列表，再仅对允许的数据源调用热点工具；不得绕过 `get_available_sources` 直接假定 source scope | P0 |
| F13-16 | 热点卡片生成后，系统必须使用 `tool_calls` 为每条新闻批量异步请求一句话描述；这些描述任务需与原 llm-chat 主回答流同时进行，单条失败不得阻塞正文输出或其它新闻项更新 | P0 |

### 3.14 F14 — 对话记录持久化

**描述**: 将 AI 对话会话和消息历史持久化到本机存储，支持应用重启后的恢复与历史管理。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F14-01 | 用户发送消息或 assistant 回复完成后，系统将最新会话内容和更新时间写入本地存储 | P0 |
| F14-02 | 应用重启后，系统恢复本地保存的会话列表、消息历史和最近一次的当前会话 | P0 |
| F14-03 | 新会话收到第一条用户消息时，系统截取该消息前 20 个字符作为标题，超过 20 字符则追加省略号 | P0 |
| F14-04 | 会话列表按 `updatedAt` 倒序展示 | P0 |
| F14-05 | 用户删除历史会话时，系统同时删除该会话的本地持久化记录 | P0 |
| F14-06 | 未登录用户在同一设备上的聊天记录仍可恢复和继续使用 | P1 |

### 3.15 F15 — MCP 设置页面重设计

**描述**: 重新设计 MCP 设置页面，采用卡片式分区布局，将模块选择从主表单移至创建 Key 时的 Modal 弹出交互，优化 Key 列表展示。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F15-01 | MCP 设置页面采用卡片式分区布局，主页面包含：顶部标题区、账号信息卡片、Key 列表区域和「创建 Key」操作入口 | P0 |
| F15-02 | 点击「创建 Key」按钮弹出 Modal，Modal 内包含：Key 名称输入、单次返回条数设置、可检索板块选择（chip grid 形式，带全选/清空快捷按钮）和「生成 API Key」确认按钮 | P0 |
| F15-03 | Modal 中填写完成并点击「生成 API Key」后调用后端 API 创建 Key，成功后关闭创建 Modal、弹出结果 Modal 展示 API Key 值和复制按钮、刷新 Key 列表 | P0 |
| F15-04 | 未填写名称就点击「生成 API Key」时显示错误提示，不关闭 Modal | P1 |
| F15-05 | Key 列表以行卡片形式展示每个 Key，包含：Key 名称、API Key 值（可点击复制）、已选模块标签（小 chip 形式）、返回条数上限、调用次数、最后调用时间、删除按钮 | P0 |
| F15-06 | 未登录用户进入 MCP 设置页面时显示登录提示，不显示 Key 列表和创建入口 | P0 |

### 3.16 F16 — API 调用量追踪

**描述**: 按小时粒度记录 MCP API 调用次数，为管理后台趋势图表提供数据源。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F16-01 | 新增 `usage` 表：id(TEXT PK)/api_key_id(TEXT FK→api_key.id)/hour(TEXT, 格式 YYYY-MM-DD HH)/call_count(INTEGER)/created_at/updated_at，(api_key_id, hour) 联合唯一 | P0 |
| F16-02 | MCP Server 启用 `Mcp-Session-Id` 会话管理（`sessionIdGenerator`），同一会话内的多次工具调用仅计一次 usage；服务端通过内存 Set 按 `hour:apiKeyId:sessionId` 去重 | P0 |
| F16-03 | `admin:getAnalytics` IPC handler 从 usage 表按时间范围查询小时级数据，填充 `hourly` 数组返回前端 | P0 |
| F16-04 | UsageRepository 封装 usage 表的 upsert 和按时间范围聚合查询 | P0 |

### 3.17 F17 — Level 权益动态配置

**描述**: 将各级别的 rate_limit / max_count 权益参数从代码硬编码迁移至 Supabase settings 表，管理员可在后台实时调整，无需改代码重新发布。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F17-01 | settings 表存储键 `level_permissions`，值为 JSON 格式的 level→权限映射（如 `{"0":{"rate_limit":3,"max_count":5},"1":{"rate_limit":20,"max_count":10},"2":{"rate_limit":-1,"max_count":50}}`），启动时自动写入默认值（INSERT ON CONFLICT DO NOTHING） | P0 |
| F17-02 | 新增 IPC 通道 `admin:getLevelPermissions`：从 settings 表读取 level_permissions JSON 并返回 | P0 |
| F17-03 | 新增 IPC 通道 `admin:updateLevelPermissions`：更新 settings 表中 level_permissions 值，并触发全量已激活用户的 API Key 权限同步 | P0 |
| F17-04 | 移除 `user-service.js`、`subscription-service.js`、`admin.handler.js` 中硬编码的 `LEVEL_PERMISSIONS` 常量，改为运行时从 SettingsRepository 读取 | P0 |
| F17-05 | 管理设置页新增「权益配置」区块，以表格形式展示各 level 的 rate_limit / max_count，支持编辑和保存 | P0 |
| F17-06 | 权益更新后，系统自动将该配置同步到所有已激活用户的 API Key（`is_active = true`） | P1 |

### 3.18 F18 — 全局外壳界面优化

**描述**: 统一应用外壳的黑白主题、中文界面和顶部导航视觉语言，确保首页、助手、设置入口与全局控制按钮具有一致的交互和样式表现。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F18-01 | 应用外壳在浅色与深色模式下均采用统一的黑白灰主题变量，允许在来源相关元素上使用轻量主题色点缀，但不得破坏整体黑白主基调 | P0 |
| F18-02 | 首页、助手、设置及其常见空状态、错误提示和主要操作文案均应以中文显示 | P0 |
| F18-03 | 顶部导航中的首页、助手、设置入口必须采用同一套 active 视觉语言；进入任意 `/settings/*` 子路由时，“设置”按钮仍需保持选中态 | P0 |
| F18-04 | 顶部“一键刷新”按钮应位于首页、助手导航入口左侧，但不得改变原有登录态与 level 权限控制逻辑 | P1 |
| F18-05 | 顶部主题切换按钮应使用无文字、无边框的纯图标圆形样式，并保留可访问说明 | P1 |

### 3.19 F19 — 首页新闻流界面优化

**描述**: 优化首页 tab 切换反馈、来源卡片辨识度与卡片内部排版，减少切换卡顿感并提升来源信息可读性。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F19-01 | 首页在热点、时事、关注和更多等板块间切换时，添加淡入淡出过渡动画（`tab-switch` transition），过渡期间不得长时间停留旧内容 | P0 |
| F19-02 | 当用户快速连续切换多个首页板块时，过期请求返回的数据不得覆盖当前板块内容 | P0 |
| F19-03 | 首页来源卡片和卡片内新闻项应基于来源配置色添加轻量背景、边框或局部点缀，以增强来源识别度 | P1 |
| F19-04 | 首页来源卡片头部中的图标、来源名称、补充 badge 和更新时间需保持垂直居中对齐 | P1 |
| F19-05 | 首页来源卡片头部与下方新闻列表之间需保持清晰但不过大的间距，避免头部和正文内容过于贴近 | P1 |

### 3.20 F20 — 设置与后台控制台界面优化

**描述**: 统一设置页导航和后台分析页的可见性反馈，确保深色模式下控件可辨识、排行榜在加载前可见且不会出现整块空白。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F20-01 | 设置页内部导航按钮应具备与应用主导航一致的清晰选中态，便于用户识别当前所在分区 | P0 |
| F20-02 | 设置页中的开关类控件在深色模式下必须保持足够对比度，避免轨道、手柄或状态颜色与背景融为一体 | P0 |
| F20-03 | 后台分析页面中的“热门 Key 排行”等统计模块在数据加载中必须显示明确的加载态或占位态，而不是整块区域留白 | P1 |
| F20-04 | 当后台排行榜数据为空时，模块应显示空状态提示，而不是直接隐藏该区域 | P1 |

### 3.21 F21 — 非阻塞启动与后台初始化

**描述**: 优化应用启动流程，已有缓存时跳过阻塞式初始化屏，直接展示缓存内容，后台异步完成 bootstrap。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F21-01 | 首次使用（无本地缓存）时保持现有阻塞式初始化屏，等待 bootstrap 完成后再加载 renderer | P0 |
| F21-02 | 已有缓存时，启动后直接加载 renderer 界面并展示缓存数据，不显示"正在初始化"加载屏 | P0 |
| F21-03 | bootstrap 在后台异步执行，期间 renderer 正常响应用户操作（浏览缓存数据、切换 tab） | P0 |
| F21-04 | bootstrap 后台执行失败时，通过 toast 通知用户，但不阻塞界面操作；界面继续展示缓存数据 | P1 |
| F21-05 | bootstrap 完成后，renderer 自动静默刷新当前 tab 的数据（复用已有缓存优先策略） | P1 |

### 3.22 F22 — 一键刷新进度反馈

**描述**: 一键刷新按钮在刷新期间显示实时进度，告知用户已刷新的数据源数量和总数。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F22-01 | 点击一键刷新后，按钮文案变为刷新中状态，显示进度"已刷新 N/M" | P0 |
| F22-02 | M 为当前用户可见的所有数据源数量（即用户偏好启用 && 管理员全局启用的数据源），N 为已完成刷新的数量 | P0 |
| F22-03 | 刷新期间按钮禁用，防止重复点击触发并发刷新 | P0 |
| F22-04 | 全部刷新完成后按钮恢复正常文案"一键刷新"，并显示成功 toast | P1 |
| F22-05 | 部分源刷新失败时，按钮恢复正常，失败信息通过 toast 提示，已成功的源数据正常更新 | P1 |

### 3.23 F07（修改） — 缓存优先数据加载（Tab 切换缓存复用）

**描述**: 扩展缓存优先策略，tab 切换时复用已加载的 boards 数据，避免每次切换都重建骨架屏。

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F07-07 | 首页 tab 切换时，若目标 tab 已有缓存的 boards 数据，直接展示已有数据，不显示骨架屏，后台静默刷新 | P0 |
| F07-08 | home-board-store 按 tab 维度独立缓存 boards、allSelectedSources 和 lastBuiltTab，确保各 tab 数据互不覆盖 | P0 |
| F07-09 | 仅在目标 tab 首次加载且无缓存时才显示骨架屏 | P0 |

---

## 4. 非功能需求

| 编号 | 需求 | 指标 |
|------|------|------|
| NF01 | 安装包大小 | 80~150 MB |
| NF02 | 冷启动时间（有缓存） | ≤ 2s（缓存优先，跳过初始化屏） |
| NF03 | 全源刷新时间 | ≤ 3min（60+ 源，并发 5） |
| NF04 | MCP 缓存命中响应 | ≤ 200ms |
| NF05 | Session 安全 | Electron safeStorage 加密存储 |
| NF06 | 代码架构 | OOP 分层（main → core → database 单向依赖） |

---

## 5. 约束

| 编号 | 约束内容 |
|------|----------|
| C01 | 首次使用需网络连接，本期不做离线模式 |
| C02 | 本期不做自动更新功能 |
| C03 | 用户等级 (level) 用于订阅方案区分和功能权限控制（level ≥ 1 可手动刷新） |
| C04 | 支付流程使用 setTimeout 模拟（3s 延迟），后续替换为真实支付网关 |
| C05 | 登录模态框仅提供亮色模式，暗色模式适配后续迭代 |
| C06 | Supabase Dashboard 需配置 redirect URL 为 `knews://auth/callback` 以支持 Magic Link |

---

## 6. 数据模型

### ER 关系

```
users (1) ────< (N) api_key
users (1) ────< (N) preference
users (1) ────< (N) subscription
source (1) ───< (N) cache
api_key (1) ──< (N) usage
```

### 表结构

#### users

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, GENERATED ALWAYS AS IDENTITY | 自增主键 |
| email | TEXT | NULLABLE | Email 用户 |
| github_id | TEXT | NULLABLE | GitHub 用户 |
| nickname | TEXT | NOT NULL | 昵称 |
| level | INTEGER | NOT NULL DEFAULT 0 | 0=普通 1=plus 2=pro |
| balance | DECIMAL(10,2) | NOT NULL DEFAULT 0 | 余额 |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() | |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() | |

#### api_key

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | |
| user_id | BIGINT | FK→users.id | |
| key_hash | TEXT | UNIQUE | |
| name | TEXT | | Key 名称 |
| is_active | BOOLEAN | DEFAULT true | |
| is_default | BOOLEAN | DEFAULT false | 内部 MCP Key 标记（注册时自动创建，不可删除） |
| source_scope | TEXT | | JSON，允许的源 |
| rate_limit | INTEGER | | 每小时限制 |
| max_count | INTEGER | | 最大返回条数 |
| last_used | TIMESTAMPTZ | | |
| call_count | INTEGER | | |
| created_at | TIMESTAMPTZ | | |
| updated_at | TIMESTAMPTZ | | |

#### source

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | 源标识 |
| name | TEXT | NOT NULL | 源名称 |
| url | TEXT | | 地址 |
| category | TEXT | | 分类 |
| enabled | BOOLEAN | DEFAULT true | |
| config | TEXT | | JSON 抓取配置 |
| created_at | TIMESTAMPTZ | | |
| updated_at | TIMESTAMPTZ | | |

#### cache

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | |
| source_id | TEXT | FK→source.id | |
| data | JSONB | | 新闻缓存数据 |
| updated_at | TIMESTAMPTZ | | |

#### preference

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | |
| user_id | BIGINT | FK→users.id | |
| data | JSONB | | 偏好 JSON |
| updated_at | TIMESTAMPTZ | | |

#### subscription

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | |
| user_id | BIGINT | FK→users.id | |
| plan | TEXT | | 'free'/'plus'/'pro' |
| level | INTEGER | | 0/1/2 |
| status | TEXT | | 'active'/'expired' |
| started_at | TIMESTAMPTZ | | 订阅开始时间 |
| expires_at | TIMESTAMPTZ | | 订阅到期时间 |
| created_at | TIMESTAMPTZ | | |
| updated_at | TIMESTAMPTZ | | |

#### usage

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | |
| api_key_id | TEXT | FK→api_key.id | |
| hour | TEXT | | UTC 小时，格式 YYYY-MM-DD HH |
| call_count | INTEGER | DEFAULT 1 | 该小时调用次数 |
| created_at | TIMESTAMPTZ | | |
| updated_at | TIMESTAMPTZ | | |

> 联合唯一约束：UNIQUE(api_key_id, hour)
