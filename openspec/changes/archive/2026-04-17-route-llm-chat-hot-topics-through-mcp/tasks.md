## 1. MCP JSON 热点数据契约

- [x] 1.1 调整本地 MCP 热点工具与聊天侧解析逻辑，确保热点卡片使用稳定的结构化 JSON 字段渲染，而不是依赖文本/markdown 解析
- [x] 1.2 为热点 payload 中的每条新闻补充可增量更新的描述字段与状态字段（如 `summary`、`summaryStatus`、稳定 `topicId`）
- [x] 1.3 同步更新 `docs/需求文档.md` 与变更内 spec，明确 MCP JSON 渲染与最小字段集合

## 2. 一句话描述并发补全

- [x] 2.1 在 `core/chat/llm-client.js` 增加适配 `tool_calls` 的请求能力，支持结构化返回单条新闻的一句话描述
- [x] 2.2 在 `core/chat/chat-service.js` 为热点列表批量异步启动描述任务，并确保它们与原 llm-chat 主回答流同时进行
- [x] 2.3 为描述补全新增独立流式事件或等价补丁机制，使单条新闻描述可以逐条回填而不阻塞正文

## 3. 前端渐进渲染与回归验证

- [x] 3.1 更新聊天 store 和 assistant 页面，仅替换数据来源与描述插槽，保持现有新闻卡片样式、交互和主题表现不变
- [x] 3.2 在描述未返回时展示标题下的加载动画，描述返回后原位替换，并验证切换会话/中断流式时状态正确
- [x] 3.3 修复热点卡片样式回归，恢复 `news-row` / 左侧数字序号布局，并确保来源标签保持在标题后
- [x] 3.4 为消息气泡补充层级化时间显示，并在 hover 时加深时间颜色
- [x] 3.5 调整 K-Ai 流式滚动体验：默认自动贴底、手动上滑后停止抢夺滚动，并提供一键到底按钮
- [x] 3.6 为 `/assistant` 增加未登录访问拦截，停留当前页并显示 warning toast
- [x] 3.7 回归验证热点卡片点击、主回答流、状态流、Thinking 展示、滚动行为与摘要并发链路可以同时工作
